# 🌟 NumPy基础学习日志  
**📅 日期**：2025年6月29日 星期日  
**☀️ 天气**：晴  
**🎯 学习主题**：NumPy基础入门  
**⏱ 学习时长**：3小时  

---

## 🚀 核心收获

### 🔢 1. 数组创建
```python
import numpy as np

# 基础数组创建
data = [1, 2, 3, 4]
arr = np.array(data)  # 列表转数组

# 特殊数组
zeros_arr = np.zeros((3,3))  # 3x3全0数组
ones_arr = np.ones(5)        # 长度5的全1数组
range_arr = np.arange(0,10,2) # 序列数组 [0,2,4,6,8]

⚡ 2. 向量化运算

python
# 高效数组运算
result = arr * 5 + 2        # 标量运算
matrix_sum = arr1 + arr2    # 数组间运算

# 性能对比
# 比Python循环快10-100倍!
🔍 3. 索引与切片
操作类型	示例	说明	效果展示
基础切片	arr[1:5]	获取1-4位置元素	[2,3,4]
多维切片	arr[0:2, 3:5]	二维数组子区域	2×2子矩阵
布尔索引	arr[arr > 3]	条件筛选	[4]
花式索引	arr[[1,3]]	获取指定索引元素	[2,4]
🧮 4. 基础函数
python
# 常用数值计算
arr.sum()       # 求和 → 10
arr.mean()      # 平均值 → 2.5
arr.max()       # 最大值 → 4

# 数组操作
reshaped = arr.reshape((2,2))  # 二维化 → [[1,2],[3,4]]
💭 学习反思与计划
🧩 当前难点
图表
代码




📚 后续重点
深入掌握广播机制(Broadcasting)

练习矩阵运算：np.dot()和@运算符

学习np.random随机数生成模块

掌握文件IO：np.loadtxt()和np.savetxt()

结合Matplotlib进行数据可视化

💡 总结感悟：
NumPy的向量化操作和高效内存管理彻底改变了科学计算的体验。短短几小时的学习已展现出其强大的数据处理能力，为数据分析奠定了坚实基础。特别欣赏它"一行代码替代多层循环"的设计哲学！

📌 学习路线：
数组创建 → 向量运算 → 索引切片 → 统计函数 → 矩阵运算 → 数据可视化